{
  "name": "propagate-delete-default",
  "description": "A delete on a fresh copy propagates even though a stale copy still has the key.",
  "baseline": ["A", "B"],
  "copies": [
    { "id": "spotify", "editedAt": 2000, "keys": ["A"] },
    { "id": "applemusic", "editedAt": 1000, "keys": ["A", "B"] }
  ],
  "expected": { "aborted": false, "merged": ["A"] }
}
